<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- PocketBase server -->
        <domain includeSubdomains="false">192.168.7.28</domain>
        <!-- PC Agent (current) -->
        <domain includeSubdomains="false">10.10.201.113</domain>
        <!-- Allow all 10.x.x.x LAN IPs so any future PC IP works without rebuild -->
        <domain includeSubdomains="true">10.0.0.0</domain>
        <!-- Allow 192.168.x.x for home/office networks -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <!-- localhost for emulator testing -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>